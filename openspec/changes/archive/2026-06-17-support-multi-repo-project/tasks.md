## 1. 模型泛化与向后兼容（纯逻辑，先测后写）

- [x] 1.1 写测试：旧单仓 `registry.json` 载入后读作「成员数为 1 的项目」，行为与扩展前一致（分组 id 复用原 entry id，唯一成员即原条目）
- [x] 1.2 在 `src/shared/types.ts` 引入 `RepoMember`（下沉原 `ProjectEntry` 的仓级字段）与 `Project { id, displayName, derivedName, members: RepoMember[] }`
- [x] 1.3 实现注册表载入迁移：把旧单仓条目包成单成员项目；新结构序列化/反序列化

## 2. registry-core：建组与成员管理（先测后写）

- [x] 2.1 写测试：用多个成员仓建一个多仓项目；单仓建项目仍为成员数 1
- [x] 2.2 写测试：`linkMember` 关联非嵌套独立仓（去重）；`unlinkMember` 只解分组、不删磁盘与 `project-id`；成员降到 0 不再作为有效项目
- [x] 2.3 写测试：成员仓凭 `project-id` 重新识别后归属原项目（移动路径/换机器复原分组）
- [x] 2.4 实现 `createProject` / `linkMember` / `unlinkMember` / 按 `project-id` 复原分组的纯函数

## 3. 主进程：嵌套子仓检测与导入编排

- [x] 3.1 写测试：容器目录自身非 git、其直接子目录含多个 git 仓时，检测出全部子仓（depth 1，跳过忽略目录）
- [x] 3.2 实现嵌套子仓扫描（复用 `probeGit`，仅当容器自身非 git 时触发）
- [x] 3.3 写测试：导入容器→确认组建多仓项目（各成员分别绑定身份）；拒绝→回退按 gitless 容器登记
- [x] 3.4 实现导入编排的多仓分支：检测→提议→确认建组 / 拒绝回退（成员仓即项目子目录，手动关联入口本期隐藏）
- [x] 3.5 gitless 成员仓在 git 出现后补绑（复用 `rebindIfGitAppeared`，把分组里该成员的标识由路径替换为 `project-id`）的测试与接线

## 4. 主进程：多根监听与磁盘对账

- [x] 4.1 `WindowManager` 对多仓项目为每个成员仓各起一个 chokidar 监听
- [x] 4.2 `FileTreeChange` 增加成员仓标识（`memberId` 或成员根路径），推给渲染层用于定位刷新
- [x] 4.3 写测试 + 实现：打开/刷新时对账成员路径——缺失成员标记 `missing`（不崩、不抛、不自动清除），缺失成员不起监听、`listDir` 失败降级为缺失
- [x] 4.4 写测试 + 实现：缺失成员凭 `project-id` 在别处重新导入时自动复位到原分组（子仓检测在导入时进行；打开时的自动再扫描留后续）

## 5. 预加载 / IPC

- [x] 5.1 扩展 IPC 与 `KlaritApi`：当前项目返回含 `members`（含各成员 `missing` 状态）的结构、关联/解绑成员仓、重新定位缺失成员、按成员订阅文件树变更
- [x] 5.2 更新共享 IPC 类型

## 6. 渲染层 UI（先测后写关键交互）

- [x] 6.1 写测试：单成员项目直接渲染文件树（无分组层）；多成员项目每个成员仓一个可折叠分组（仓名 + 分支）
- [x] 6.2 实现 `RepoGroup` 分组组件并在 `Sidebar` 按 `members.length` 选择渲染方式
- [x] 6.3 写测试：缺失成员显示为「缺失/不可用」并提供「重新定位 / 从项目移除」；成员分组「解绑」入口触发回调；不再展示「关联成员仓」入口
- [x] 6.4 实现成员解绑与缺失成员复位的轻量入口（手动关联入口本期隐藏，成员仓即项目子目录）；切换器仍按项目名展示（回归测试不破单仓行为）

## 7. 端到端与收尾

- [x] 7.1 e2e：导入含 `frontend`、`backend` 两子仓的容器 → 确认组建 → 侧边栏出现两个成员仓分组、各自文件树
- [x] 7.2 e2e：关闭重开，多仓项目按 userData 分组复原
- [x] 7.3 e2e：关联 / 解绑一个成员仓，侧边栏分组随之增减（解绑不删磁盘）
- [x] 7.6 e2e：外部删除一个成员目录 → 该成员显示缺失、其它成员照常、项目不丢（「移到新路径重新导入→自动复位」由单测 `relocateMemberToDir` 与 `upsertImport` 移动路径复用覆盖）
- [x] 7.4 回归：单仓项目导入/展示/切换/恢复行为不变（含旧 registry.json 兼容）
- [x] 7.5 全量 `npm run typecheck` / `npm run test:run` / `npm run test:e2e` 通过，自检后请用户验收
