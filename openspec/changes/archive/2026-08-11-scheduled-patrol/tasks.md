# Tasks

> 设计已定：形态照搬 `auto-scheduler`（串行化、永不抛、注入式）；触发只有三种固定周期无 cron；动作全复用既有接缝；产出只进收件箱或推候选卡、绝不自作主张；共享并发槽、槽满跳过不排队；软件没开不跑、开机只补一次。
>
> **依赖 `decision-inbox` 先落地**（产出的一个去处）。

## 1. 巡检模型与到期判定（纯函数，shared）

- [x] 1.1 写测试：`isDue({ trigger, lastRunAt, now })` —— `everyHours` 满/未满；`daily` 今天已过时刻且 lastRunAt 是昨天 → 到期；`weekly` 同理
- [x] 1.2 写测试：`lastRunAt` 缺省 → 到期（首次立即跑）；巡检停用 → 恒不到期
- [x] 1.3 写测试：`isDue` 不依赖真实时钟（`now` 由入参给），同输入恒同输出
- [x] 1.4 写测试：错过多个窗口 → `isDue` 仍只答「到期」（补跑次数由回路控制，不由判定函数表达）
- [x] 1.5 实现 `src/shared/patrol.ts`（`Patrol` 类型 + `isDue` + 触发的人话描述格式化）

## 2. 巡检配置持久化

- [x] 2.1 写测试：新建/编辑/删除/启停 → 重开后保持；`lastRunAt` 持久化
- [x] 2.2 写测试：项目无巡检字段（老项目）→ 读为空列表、不报错
- [x] 2.3 实现：`project-service.ts` 巡检读写 + IPC

## 3. 常驻巡检回路（主进程）

- [x] 3.1 写测试：到期 → 发起动作并更新 `lastRunAt` 为发起时刻；未到期 → 不发起
- [x] 3.2 写测试：**动作失败仍更新 `lastRunAt`**（不在本 tick 重试）
- [x] 3.3 写测试：评估串行化 —— 评估中再触发 → 合并为一次补评估，同一巡检不双发
- [x] 3.4 写测试：单次评估抛异常 → 回路存活，后续 tick 照常
- [x] 3.5 写测试：开机时错过 12 个窗口 → **只发起一次**
- [x] 3.6 实现 `src/main/patrol-scheduler.ts`（注入 `listPatrols`/`getProject`/`isRunLive`/各动作执行器/产出去处；与 `auto-scheduler.ts` 同风格）

## 4. 三类动作映射到既有接缝

- [x] 4.1 写测试：工作流动作 → 走与自动排程相同的运行启动路径（断言复用同一接缝，非新起一套）
- [x] 4.2 写测试：命令动作 → 走既有可取消命令运行器，输出与中止语义一致
- [x] 4.3 写测试：文档扫描动作 → 调既有 `document-scan`
- [x] 4.4 实现：动作分派（薄适配，零新执行器）

## 5. 产出去处 + 不自作主张边界

- [x] 5.1 写测试：发现该改的东西 → 经既有候选提交接缝推候选卡，**止于审阅**
- [x] 5.2 写测试（边界，重点）：巡检完成后 **断言**未改代码、未合并分支、未落库活卡、未为产出排程运行
- [x] 5.3 写测试（边界，重点）：**断言巡检不向决策收件箱写任何独立条目**（`decision-inbox` 是纯投影，塞条目会破坏其不变量）
- [x] 5.4 写测试：**断言巡检运行不进收件箱**（不绑卡，收件箱按 `card.activeRunId` 反查界定）

## 5b. 巡检运行抛决策即中止转候选（补：修 spec 漏洞，防静默卡住 + 槽位泄漏）

- [x] 5b.1 写测试：巡检拉起的运行抛出**人工门**决策 → 系统 `abort` 该运行 + 推出载明巡检名/节点/决策说明的候选卡
- [x] 5b.2 写测试：抛出**引擎失败决策**（非人工门）→ 同样中止并转候选（不分来源）
- [x] 5b.3 写测试：中止后该运行**不再计入活跃数**、槽位可被自动排程或后续巡检使用
- [x] 5b.4 写测试：**断言巡检运行绝不停在 `waiting-decision` 上**
- [x] 5b.5 实现：巡检回路监听自己拉起的运行的决策事件 → `abort` + `pushCandidates`
- [x] 5.5 写测试：巡检推出的未审阅候选 → 自动排程不拉起
- [x] 5.6 实现：产出分派（只注入 `pushCandidates`，**不注入任何 inbox 写入口**）

## 6. 并发槽共享

- [x] 6.1 写测试（`auto-scheduler`）：巡检拉起的活跃运行计入活跃数、空槽相应减少
- [x] 6.2 写测试：巡检占满槽 → 自动排程本次不拉起任何卡
- [x] 6.3 写测试：槽满时巡检到期 → **跳过本次**、`lastRunAt` 照常更新、**不排队**
- [x] 6.4 写测试：手动启动仍不受上限约束
- [x] 6.5 实现：`auto-scheduler.ts` 活跃计数纳入巡检运行；巡检回路发起前查槽

## 7. 设置界面

- [x] 7.1 写测试：列出巡检（名字/触发人话描述/动作/开关/上次运行）；停用项有明确视觉；空态与新建入口
- [x] 7.2 写测试：触发编辑为下拉 + 时刻选择器，**断言无表达式输入框**
- [x] 7.3 实现：巡检设置界面 + i18n（语义令牌、深浅两套，按 `docs/brand`）

## 8. 收尾

- [x] 8.1 `npm run typecheck` 两套干净、`npm run test:run` 全绿
- [x] 8.2 `npx openspec validate scheduled-patrol --strict`
- [ ] 8.3 dogfood（2026-08-31 真机 e2e 覆盖了一半，见 `e2e/dogfood-acceptance.spec.ts`）：
      ✅ 到期真发起（`everyHours: 0.001` + 主进程分钟级 tick，lastRunAt 被记上）
      ✅ 重开只补一次（停用→启用后 lastRunAt 只前进一次，不排队补齐停用期间欠的次数）
      ⬜ 产出进收件箱/候选 —— 未验
      ⬜ 槽满跳过不排队 —— 未验（要先把并发槽占满，e2e 里没造这个场景）
